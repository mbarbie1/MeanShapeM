
% parameters
folder_list = {};
image_id = 'B38_66';
roi_name = 'th';

% in the loop
roi_file_name = fullfile( image_id, '.zip' );
folder = ;
roi_file_path = fullfile( image_id, '.zip' );

%function masks = read_named_roi( folder_list, image_id, roi_name )
%%READ_ROIS Reads the ROIs from all image_id's, with a specific name from ImageJ zip files.
%%           If the ROIs 
%%   Detailed explanation goes here
%    for i = 1:folder_list
%end

